# tirra

A minimal Hugo theme for a digital garden.

## Usage

Add to `hugo.toml`:

```toml
theme = "tirra"
```

## Configuration

See [theme.toml](theme.toml) for theme metadata and [hugo.toml](../../hugo.toml) for site configuration.
